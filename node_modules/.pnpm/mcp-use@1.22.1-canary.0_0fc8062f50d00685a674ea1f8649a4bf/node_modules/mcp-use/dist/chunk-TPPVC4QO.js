import {
  __name
} from "./chunk-3GQAWCBQ.js";

// src/server/utils/jsonrpc-helpers.ts
function createNotification(method, params) {
  return {
    jsonrpc: "2.0",
    method,
    ...params && { params }
  };
}
__name(createNotification, "createNotification");
function createRequest(id, method, params) {
  return {
    jsonrpc: "2.0",
    id,
    method,
    ...params && { params }
  };
}
__name(createRequest, "createRequest");
function isJsonRpcResponse(msg) {
  return !!msg && typeof msg === "object" && "id" in msg && ("result" in msg || "error" in msg);
}
__name(isJsonRpcResponse, "isJsonRpcResponse");
function isJsonRpcRequest(msg) {
  return !!msg && typeof msg === "object" && "id" in msg && "method" in msg && !("result" in msg) && !("error" in msg);
}
__name(isJsonRpcRequest, "isJsonRpcRequest");

export {
  createNotification,
  createRequest,
  isJsonRpcResponse,
  isJsonRpcRequest
};

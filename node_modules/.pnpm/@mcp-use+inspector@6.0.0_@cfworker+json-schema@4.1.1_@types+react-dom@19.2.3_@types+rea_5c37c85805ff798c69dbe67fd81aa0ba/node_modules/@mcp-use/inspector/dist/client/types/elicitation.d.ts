import type { ElicitRequestFormParams, ElicitRequestURLParams, ElicitResult } from "@modelcontextprotocol/sdk/types.js";
export interface PendingElicitationRequest {
    id: string;
    request: ElicitRequestFormParams | ElicitRequestURLParams;
    timestamp: number;
    serverName: string;
    toolName?: string;
}
export interface ElicitationRequestWithHandlers extends PendingElicitationRequest {
    resolve: (result: ElicitResult) => void;
    reject: (error: Error) => void;
}
export declare const DEFAULT_ELICITATION_CANCEL: ElicitResult;
//# sourceMappingURL=elicitation.d.ts.map
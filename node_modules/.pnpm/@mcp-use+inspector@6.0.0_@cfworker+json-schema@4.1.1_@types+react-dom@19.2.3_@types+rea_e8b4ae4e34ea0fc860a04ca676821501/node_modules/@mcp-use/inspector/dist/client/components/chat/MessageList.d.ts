import type { MessageAttachment } from "./types";
import type { PendingElicitationRequest } from "@/client/types/elicitation";
import type { ElicitResult } from "@modelcontextprotocol/sdk/types.js";
interface Message {
    id: string;
    role: "user" | "assistant";
    content: string | Array<{
        index: number;
        type: string;
        text: string;
    }>;
    timestamp: number;
    attachments?: MessageAttachment[];
    parts?: Array<{
        type: "text" | "tool-invocation";
        text?: string;
        toolInvocation?: {
            toolName: string;
            args: Record<string, unknown>;
            result?: any;
            state?: "pending" | "streaming" | "result" | "error";
            partialArgs?: Record<string, unknown>;
        };
    }>;
    toolCalls?: Array<{
        toolName: string;
        args: Record<string, unknown>;
        result?: any;
    }>;
}
interface MessageListProps {
    messages: Message[];
    isLoading: boolean;
    serverId?: string;
    readResource?: (uri: string) => Promise<any>;
    tools?: any[];
    sendMessage?: (message: string, attachments?: MessageAttachment[]) => Promise<void>;
    /** When provided, passed to widget renderers to avoid useMcpClient() context lookup. */
    serverBaseUrl?: string;
    /** Pending elicitation requests to render inline in the chat thread. */
    pendingElicitationRequests?: PendingElicitationRequest[];
    /** Handler called when the user accepts or declines an elicitation. */
    onApproveElicitation?: (requestId: string, result: ElicitResult) => void;
    /** Handler called when the user cancels an elicitation. */
    onRejectElicitation?: (requestId: string, error?: string) => void;
}
export declare const MessageList: import("react").MemoExoticComponent<({ messages, isLoading, serverId, readResource, tools, sendMessage, serverBaseUrl, pendingElicitationRequests, onApproveElicitation, onRejectElicitation, }: MessageListProps) => import("react/jsx-runtime").JSX.Element>;
export {};
//# sourceMappingURL=MessageList.d.ts.map